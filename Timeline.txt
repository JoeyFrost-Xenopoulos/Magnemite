Data is preprocessed using K-means clustering (Kmean_preprocessing_3)
	- An automatic algorithm is coded to figure out ideal cluster
	- This process gives us the tooth distribution which are the times
	- Attempts were made to code a fully automatic algorithm but all failed
	- Automatic algorithm for subsetting both top and bot time ticks

Times are discovered using hierarchical clustering
	- This is when brush clust was coded up
	- This is when timing tick click was coded
	- Run the kmeans algo
	- Brush over teeth and that gives us a valid subset of the data
	- Hier clus is applied to further subdivide into more precise clusters
	- These teeth are actually hundreds of small datapoints, so locating the full group
		is diff. That is why hier clus is used
	- We then get a fully accurate with some manual use times

Times are turned into actual_times
Fill in the data in between
Fix the end issue
Then using gpcm we cluster
	- Meat and potatoes here
	- I need to do a bit more work to get a better output from this